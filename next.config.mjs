/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "oyseuwymcaieaouvdgzf.supabase.co",
      },
    ],
  },
};

export default nextConfig;
